{
  "name": "fumika-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core",
    "dev:ide": "npm run dev -w packages/ide",
    "dev:playground": "concurrently \"npm run dev -w packages/core\" \"npm run dev -w packages/playground\"",
    "build:playground": "npm run build -w packages/playground",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^6.0.2"
  }
}